# Device-Driver-for-ATmeg328P
Bare-metal device driver implementation for the ATmega328P (Arduino UNO), including GPIO and UART drivers written using direct register-level programming without Arduino libraries.
